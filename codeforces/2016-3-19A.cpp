#include <iostream>
#include <stdio.h>
#include <string.h>
#include <string>
#include <queue>
#include <stack>

using namespace std;
char m[2222][2222];
int num[2222];
int sum[2222][2222];

void solve()
{
    int <queue> q;
    for(int i=1;i<=n;i++)
        q.push
}


int main()
{
    int n;
    while(~scanf("%d",&n))
    {
        getchar();
        for(int i=0;i<n;i++)
            scanf("%s",m[i]);
        for(int i=0;i<n;i++)
            for(int j=0;j<n;j++)
                //cout<<m[i][j]<<" ";
        {
                    num[i]++;
                    sum[i][j]++:
                    sum[i+j]++:
                    sum[i][j]++:
        }
        int <queue> ;

        //shuchu hang
        printf("h %d %c",ans,cc);
        //shuchulie
        printf("v %d %c",ans,cc);

    }
    return 0;
}
