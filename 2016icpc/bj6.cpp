#include <cstdio>
#include <iostream>
using namespace std;
long long minus[1<<21];
long long a[111111];
long long b[111111];
long long sum[111111];

int main()
{
    int T;
    scanf("%d",&T);
    while(T--)
    {
    	int n;
    	scanf("%d",&n);
    	for(int i=1;i<=n;i++)
    		scanf("%lld",&a[i]);
    	for(int i=1;i<=n;i++)
    		scanf("%lld",&b[i]);

    	for(int i=1;i<=n;i++)
    		for(int j=i+k;)

    }
    return 0;
}